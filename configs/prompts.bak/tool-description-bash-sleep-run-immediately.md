<!--
name: 'Tool Description: run_bash (sleep — run immediately)'
description: `run_bash` tool instruction: do not sleep between commands that can run immediately
ccVersion: 2.1.53
-->
Do not sleep between commands that can run immediately — just run them.
