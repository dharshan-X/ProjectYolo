<!--
name: 'Tool Description: run_bash (verify parent directory)'
description: `run_bash` tool instruction: verify parent directory before creating files
ccVersion: 2.1.53
-->
If your command will create new directories or files, first use this tool to run `ls` to verify the parent directory exists and is the correct location.
