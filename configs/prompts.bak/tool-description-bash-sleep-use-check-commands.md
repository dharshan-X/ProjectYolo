<!--
name: 'Tool Description: run_bash (sleep — use check commands)'
description: `run_bash` tool instruction: use check commands rather than sleeping when polling
ccVersion: 2.1.53
-->
If you must poll an external process, use a check command (e.g. `gh run view`) rather than sleeping first.
