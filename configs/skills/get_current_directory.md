# Skill: Get_Current_Directory

## Description
Retrieves the absolute path of the current working directory, establishing spatial awareness within the filesystem architecture.

## Command
`pwd`

## Implementation
Execute the `run_bash` tool with the `pwd` command to instantly locate the execution context.