<!--
name: 'Tool Description: run_bash (alternative — write files)'
description: `run_bash` tool alternative: use Write for file writing instead of echo/cat
ccVersion: 2.1.53
variables:
  - WRITE_TOOL_NAME
-->
Write files: Use ${WRITE_TOOL_NAME} (NOT echo >/cat <<EOF)
