<!--
name: 'Tool Description: run_bash (sleep — no polling background tasks)'
description: `run_bash` tool instruction: do not poll background tasks, wait for notification
ccVersion: 2.1.53
-->
If waiting for a background task you started with `run_in_background`, you will be notified when it completes — do not poll.
